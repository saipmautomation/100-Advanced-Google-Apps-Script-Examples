# 006. Atomic log writer (prevents collisions)

**What it does:** Uses LockService so concurrent executions don’t collide.

**Requirements / Notes:** Sheets-bound script.

## Code
See [`Code.gs`](./Code.gs)
