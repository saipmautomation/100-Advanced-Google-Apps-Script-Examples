# 082. Avoid duplicates: only create event if not already there

**What it does:** Creates an event only if it doesn’t already exist in time window.

**Requirements / Notes:** Calendar access.

## Code
See [`Code.gs`](./Code.gs)
