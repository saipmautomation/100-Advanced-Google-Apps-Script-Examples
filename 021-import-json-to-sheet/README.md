# 021. Import JSON into sheet (flatten basic objects)

**What it does:** Fetches JSON and writes key/value pairs to the active sheet.

**Requirements / Notes:** Sheets-bound script + UrlFetch permissions.

## Code
See [`Code.gs`](./Code.gs)
