# 038. Create “safe” unique filenames

**What it does:** Returns a unique filename that doesn’t already exist in the folder.

**Requirements / Notes:** Use from other functions. Replace folder ID when calling.

## Code
See [`Code.gs`](./Code.gs)
