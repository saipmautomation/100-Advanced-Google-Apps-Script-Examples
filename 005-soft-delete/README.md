# 005. Soft delete rows (flag instead of deleting)

**What it does:** Marks a row as deleted with a flag + note (audit-friendly).

**Requirements / Notes:** Sheets-bound script. Adjust `row` as needed.

## Code
See [`Code.gs`](./Code.gs)
