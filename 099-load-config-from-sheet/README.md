# 099. Centralized config loader (sheet → object)

**What it does:** Loads key/value config from a Config sheet (columns A:B).

**Requirements / Notes:** Sheets-bound script.

## Code
See [`Code.gs`](./Code.gs)
