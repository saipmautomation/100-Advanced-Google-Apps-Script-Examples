# 094. HTMLService UI + server call (google.script.run)

**What it does:** Shows a modal dialog UI and calls server function to return text.

**Requirements / Notes:** Sheets-bound script (for UI).

## Code
See [`Code.gs`](./Code.gs)
