# 031. Rename files with a consistent prefix

**What it does:** Adds a prefix to all files in a folder if missing.

**Requirements / Notes:** Replace `PASTE_FOLDER_ID`.

## Code
See [`Code.gs`](./Code.gs)
