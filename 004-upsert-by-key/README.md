# 004. Upsert rows by key (update or append)

**What it does:** Updates an existing row when key matches; otherwise appends.

**Requirements / Notes:** Sheets-bound script. Edit `key`, `payload`, `keyCol`.

## Code
See [`Code.gs`](./Code.gs)
