# 100. End-to-end: fetch → store → report → email

**What it does:** Fetches JSON, writes to sheet, then emails a completion summary.

**Requirements / Notes:** Gmail + Sheets + UrlFetch permissions.

## Code
See [`Code.gs`](./Code.gs)
