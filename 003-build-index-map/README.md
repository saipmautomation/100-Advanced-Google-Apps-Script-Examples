# 003. Build an index of unique keys → row numbers

**What it does:** Builds a map from key values (col A) to row number for fast lookups.

**Requirements / Notes:** Sheets-bound script.

## Code
See [`Code.gs`](./Code.gs)
