# 047. Replace placeholders in a Doc (multiple fields)

**What it does:** Replaces multiple placeholder tokens in active doc.

**Requirements / Notes:** Docs-bound script (recommended).

## Code
See [`Code.gs`](./Code.gs)
