# 064. Mail-merge to PDFs (Doc template + sheet rows)

**What it does:** Creates personalized docs from a template + sheet rows, exports PDFs, saves to folder.

**Requirements / Notes:** Replace template + folder IDs. Drive/Docs/Sheets access. Creates PDFs.

## Code
See [`Code.gs`](./Code.gs)
