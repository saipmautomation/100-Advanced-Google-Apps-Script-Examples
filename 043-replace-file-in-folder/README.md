# 043. Replace a file in a folder (version-style)

**What it does:** Trashes existing named file(s) then creates a new one with same name.

**Requirements / Notes:** Replace `PASTE_FOLDER_ID` and `name` as desired. ⚠️ Destructive.

## Code
See [`Code.gs`](./Code.gs)
